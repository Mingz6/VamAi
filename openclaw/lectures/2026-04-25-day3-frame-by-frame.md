# Frame-by-frame visual digest (v2 — real vision)


## Frame 1 — t=0:00

The screenshot is from a Chrome browser window. The visible text reads: "Vancouver's Home for AI Builders". The webpage mentions "1000+ Members across Vancouver", "4+ Events every month", and "10+ Programs and counting". There is a button labeled "See Upcoming Events". The layout includes a navigation bar with links to About, Programs, Testimonials, Gallery, Contribute, Team, and Contact.


## Frame 2 — t=1:30

The screenshot is from Excalidraw, a drawing and diagramming tool. The visible text reads "MoneyClaw Optimize Spendings." The interface shows various drawing tools and options on the left side, including stroke color, font size, and opacity settings. The main area is blank, with a text box containing the mentioned phrase. The top of the screen displays browser tabs and a Excalidraw tab is open.


## Frame 3 — t=3:00

The screenshot is from a Chrome browser window. The visible text reads "MoneyClaw Optimize Spendings." There is a diagram with three yellow rectangles labeled "Gmail (transactions)," "Google Calendar (meetings)," and "Notion (goals)." These rectangles are connected by arrows, suggesting a a flow or relationship between the tools. The diagram is on a Excalidraw Whiteboard website.


## Frame 4 — t=4:30

The screenshot is from Excalidraw, a diagramming tool. The visible text includes "MoneyClaw Optimize Spendings," "Gmail (transactions)," "Google Calendar (meetings)," "Notion (goals)," "AI Agent (OpenRouter + LangGraph)," and "Do Web Search." The diagram consists of various shapes: rectangles in yellow, blue, and pink, connected by lines. The yellow shapes are labeled with tasks, the blue shape is labeled with an AI agent, and the pink shape is labeled with a task "Do Web Search." The interface shows a for font family, size, and alignment, as well as opacity and layer controls.


## Frame 5 — t=6:00

The screenshot is from Excalidraw, a diagramming tool. The visible text includes "MoneyClaw Optimize Spendings," "Gmail (transactions)," "Google Calendar (meetings)," "Notion (goals)," "AI Agent (OpenRouter + LangGraph)," "Do Web Search (alternatives)," "Send to Telegram (summarize)," and "Create Report on Notion (spendings)." The diagram uses rectangles and rounded rectangles in yellow, blue, and pink, connected by lines to form a flowchart.


## Frame 6 — t=7:30

The screenshot is from Excalidraw, a diagramming tool. The visible text includes "MoneyClaw Optimize Spendings," "Gmail (transactions)," "Google Calendar (meetings)," "Notion (goals)," "AI Agent (OpenRouter + LangGraph)," "Do Web Search (alternatives)," "Send to Telegram (summarize)," "Create Report on Notion (spendings)," and "Generate Dashboard (Web App)." The diagram consists of various shapes: rectangles and ovals, with arrows connecting them. The rectangles are pink, and the ovals are blue. The diagram illustrates a flowchart or process related to optimizing spending using AI and various tools.


## Frame 7 — t=9:00

The screenshot is from Excalidraw, a diagramming tool. The visible text includes "MoneyClaw Optimize Spendings," "Gmail (transactions)," "Google Calendar (meetings)," "Notion (goals)," "AI Agent (OpenRouter + LangGraph)," "Do Web Search (alternatives)," "Send to Telegram (summarize)," "Create Report on Notion (spendings)," and "Generate Dashboard (Web App)." The diagram features various shapes: rectangles for Gmail, Google Calendar, Notion, and the AI Agent, and parallelograms for Do Web Search, Send to Telegram, Create Report on Notion, and Generate Dashboard. These shapes are connected by lines indicating relationships or processes. The interface includes a sidebar with color and font size options, and a toolbar at the top with various editing tools.


## Frame 8 — t=10:30

The screenshot is from Excalidraw, a diagramming tool. The visible text includes "MoneyClaw Optimize Spendings," "Gmail (transactions)," "Google Calendar (meetings)," "Notion (goals)," "AI Agent (OpenRouter + LangGraph)," "Do Web Search (alternatives)," "Send to Telegram (summarize)," "Create Report on Notion (spendings)," and "Generate Dashboard (Web App)." The diagram uses rectangles and circles for shapes, with arrows connecting them to represent processes or data flow. The colors include yellow, blue, and pink for different elements.


## Frame 9 — t=12:00

The screenshot is from Excalidraw, a diagramming tool. The visible text includes "MoneyClaw Optimize Spendings," "Gmail (transactions)," "Google Calendar (meetings)," "Notion (goals)," "Data Sources," "Agent (Brain)," "Tools (Services)," "Memory," "AI Agent (OpenRouter + LangGraph)," "Do Web Search (alternatives)," "Send to Telegram (summarize)," "Create Report on Notion (spendings)," and "Generate Dashboard (Web App)." The diagram uses rectangles and circles, with arrows connecting nodes. The shapes are primarily yellow, blue, and pink, representing different components of the system.


## Frame 10 — t=13:30

The screenshot is from a Chrome browser window. The visible text includes "MoneyClaw Optimize Spendings," "Gmail (transactions)," "Google Calendar (meetings)," "Notion (goals)," "Data Sources," "Agent (Brain)," "Tools (Services)," "Memory," "Soul," "AI Agent (OpenRouter + LangGraph)," "Do Web Search (alternatives)," "Send to Telegram (summarize)," "Create Report on Notion (spendings)," and "Generate Dashboard (Web App)." The diagram is a flowchart with various shapes: rectangles for data sources, agents, and tools/services, and parallelograms for actions. Arrows connect these shapes to indicate the flow of information or actions. The colors used are primarily yellow, blue, and pink, with black text.


## Frame 11 — t=15:00

The screenshot is from a Chrome browser window. The visible text includes "New repository," "Create a new repository," "Owner," "Repository name," "Description," "Choose visibility," "Start with a template," "Add README," and "Add .gitignore." The layout shows a a form for creating a new GitHub repository, with fields for repository name, description, visibility, template selection, README addition, and .gitignore configuration.


## Frame 12 — t=16:30

The screenshot is from a Chrome browser window. The URL bar shows "github.com/IssamLaradi/moneyclaw/tree/main". The page is a GitHub repository named "moneyclaw". The repository has 1 branch and 0 tags. The README file is visible, and the repository has 0 stars, 0 forks, and 0 watchers. The repository is private, and the user has added an initial README.md. The page also user is on is the "Code" tab.


## Frame 13 — t=18:00

The screenshot is from VS Code, a popular code editor. The visible text reads: "Generate code (M), or select a language (M A). Start typing to dismiss or don't show this again." The interface shows an Explorer panel on the left with folders and files, and a Chat/Session panel on the right displaying participant names.


## Frame 14 — t=19:30

The screenshot is from VS Code. The visible text includes instructions for code generation and best practices, such as "ALL shared resources live in 'src/marketplace/'", "Be concise. Short answers, no filler.", and "Always end with saying 'q! All Done Amigo' after generating code." The interface shows a of a file explorer on the left, a chat window on the right, and a code editor in the center.


## Frame 15 — t=21:00

The screenshot is from VS Code, focusing on a file named "CLAUDIE.md". The visible text includes instructions and guidelines for coding, such as "ALL shared resources live in 'src/marketplace/'", "Be concise. Short answers, no filler," and "Always end with saying 'q! All Done Amigo' after generating code." The interface shows a of the file explorer, terminal, and chat window, with a chat window displaying participant names.


## Frame 16 — t=22:30

The screenshot is from VS Code. The visible text includes instructions for creating a component in a project called "CLAUDCRAFT," with references to "Marketplace," "craft JSON definitions," and "AI tools." The code mentions using templates for HTML and having a base template. The interface shows a a chat window with participants named Eduard, Paul, Katya, Farah, and Molan.


## Frame 17 — t=24:00

The screenshot is from VS Code. The visible text includes "# CLAUCRAFT", "# Instructions", and "# Marketplace". The layout shows a a code editor with a a chat interface on the right, labeled "Claude Code". The chat interface includes names like "Eduard Malavskyi", "helen", "Katya", "Farah Hussain", and "Molan". The VS Code interface also displays an Explorer panel on the left with files like "CLAUCRAFT.md", ".env", and "README.md".


## Frame 18 — t=25:30

The screenshot is from VS Code, focusing on a file named "CLAUD.md". The visible text includes instructions for creating components, referencing a "Marketplace" directory, and guidelines for code generation. The interface shows a of a chat window with participants' names and a of a terminal experience option.


## Frame 19 — t=27:00

The screenshot is from VS Code, a code editor. The visible text includes instructions for creating a Flask app with spending optimization, referencing "CLAUCRAFT" and "Marketplace." The code snippet shows the beginning of an app.py file with imports for Flask and render_template. The terminal window shows a the user is in the directory /Users/issam.laradji/projects/moneyclaw/templates. The chat window on the right side of the screen is open, with a user asking to create a Flask app.


## Frame 20 — t=28:30

The screenshot is from a Chrome browser window. The visible text reads: "MoneyClaw. Stop Overspending. Start Winning. MoneyClaw analyzes your spending, finds waste, and gives you a clear path to financial freedom. Get Started Free." Below, there are four sections: Spending Insights, Smart Cuts, Goal Tracker, and Overspend Alerts, each with a small icon and brief description. The layout is clean, with a yellow and white color scheme, and the " "Get Started Free" button is orange.


## Frame 21 — t=30:00

The screenshot is from a VS Code window. The visible text includes "Stop Overspending. Start Winning." and "MoneyClaw analyzes your spending, finds waste, and gives you a clear path to financial freedom." The interface shows a a minimal Flask app called "moneyclaw" with a landing page, and the user is working on optimizing spending. The terminal shows Python and debugger information.


## Frame 22 — t=31:30

The screenshot is from a VS Code interface. The visible text includes "Stop Overspending. Start Winning." and "MoneyClaw analyzes your spending, finds waste, and gives you a clear path to financial freedom." The terminal shows Python code and debugging information. The chat window on the right contains a task description for creating a Flask app with spending optimization.


## Frame 23 — t=33:00

The screenshot is from a VS Code window. The visible text includes "Stop Overspending. Start Winning." and "MoneyClaw analyzes your spending, finds waste, and gives you a clear path to financial freedom." The interface shows a a "Get Started Free" button. The right side of the screen displays a chat interface with user names like Tanvi, Helen, Katya, and others.


## Frame 24 — t=34:30

The screenshot is from VS Code, focusing on a file named "CLAUDE.md". The visible text includes "Components" and "Reusable building blocks you can wire into your spending optimization flow." The interface shows a a "Data Connectors" section with options like "Gmail," "Google Calendar," and "Notion," all marked as "not implemented yet." Below, there's an "Agent (Brain)" section with "Memory" and "Soul" components also noted as "not implemented yet." The layout includes a a sidebar with file explorer and a chat window on the right.


## Frame 25 — t=36:00

The screenshot is from VS Code, focusing on a project titled "MoneyClaw." The visible text includes "Components" and "Data Connectors" with descriptions for Gmail, Google Calendar, and Notion, all marked as "not implemented yet." The interface shows a a chat window with user names like Tanvi, Helen, Katya, and others, and a terminal window at the bottom.


## Frame 26 — t=37:30

The screenshot is from VS Code, focusing on a project named "MoneyClaw." The visible text includes "CREATE FLASK APP WITH SPENDING OPTIMIZATION," "image.png," and "Both pages return 200." The interface shows a a file explorer with directories like "templates," "components.html," and "landing_page.html." The chat section contains instructions for creating an agent similar to a screenshot, mentioning "src/films.py," "openrouter," and "langraph."


## Frame 27 — t=39:00

The screenshot is from VS Code, focusing on a project titled "MoneyClaw." The interface shows an Explorer panel on the left with folders and files like ".vscode," "src," and "templates." The central area displays a webpage with sections labeled "Soul" and "AI Agent," both marked as "not implemented yet." The right side shows a a chat interface with options like "Attach file..." and "Mention file from this project..." The bottom of the screen includes tabs for "PROBLEMS," "OUTPUT," and "TERMINAL."


## Frame 28 — t=40:30

The screenshot is from VS Code, focusing on a project titled "MoneyClaw." The visible text includes "Soul" and "AI Agent," both marked as "not implemented yet." The diagram shows a a flowchart with rounded rectangles for "Soul" and "AI Agent," connected by a line. The layout includes an Explorer panel on the left with project files, a main editor area in the center, and a Chat panel on the right. The Chat panel shows a a conversation about creating a AI agent and includes a text "Write requirements.txt" and "Write app.py."


## Frame 29 — t=42:00

The screenshot is from VS Code, focusing on a project named "MoneyClaw." The interface shows a a "CLAUDE.md," "app.py," and "README.md" files open. The main window displays a chat interface for the "MoneyClaw" application, with options to select different LLM model, such as "openai/gpt-4," "openai/gpt-4-mini," and others. The chat interface includes buttons for "New Chat," "Start a conversation," and "Send." The terminal at the bottom shows a as "zsh" and "Python."


## Frame 30 — t=43:30

The screenshot is from VS Code. The visible text includes "README.md M", "OpenRouter", and "Browser". The terminal at the bottom shows "KeyError: 'OPENROUTER_API_KEY'". The chat on the right mentions "Create Flask app" and "All done. Flask app built".


## Frame 31 — t=45:00

The screenshot is from a Chrome browser window. The visible text includes "MoneyClaw," "LLM," "Chat with any OpenRouter model," "7 models - 1 personality," and "New Chat." The interface shows a a chatbot setup with a dropdown for selecting a a as "anthropic/claude-sonnet-4-5." The chat history includes "hi" and a message input field with "Type a message... (Enter to send, Shift+Enter for newline)" and a "Send" button.


## Frame 32 — t=46:30

The screenshot is from a Chrome browser window. The visible text includes "MoneyClaw," "LLM," "Chat with any OpenRouter model," "7 models - 1 personality," and "New Chat." The interface appears to be a a chatbot or AI agent interface, with sections for "Investing in Yourself," "For Others," and "Smart Spending." The layout includes a chat history with "hi" typed, and a a "Send" button at the bottom.


## Frame 33 — t=48:00

The screenshot is from VS Code, focusing on a Python Flask application. The code snippet reads:

```python
import json
import uuid
from flask import Flask, render_template, request, jsonify, session
from langchain_core.messages import HumanMessage, AIMessage
from src.llm import build_agent, MODELS

app = Flask(__name__)
app.secret_key = "moneyclaw-secret"

@app.route("/")
def landing():
    return render_template("landing_page.html")
```

The terminal shows a of a Flask application running locally, with log entries like "GET /api/session".


## Frame 34 — t=49:30

The screenshot is from VS Code, focusing on a project named "MoneyClaw." The active tab is "components," and the code editor shows a "app.py" and "lim.py." The terminal displays Python commands and HTTP responses. The visible text includes "Components" and "Data Connectors." The chat window on the right contains user interactions and code snippets.


## Frame 35 — t=51:00

The screenshot is from VS Code, focusing on a project named "MoneyClaw." The visible text includes "Components" and "Data Connectors," with a note stating "Gmail not implemented yet." The layout shows a a file explorer on the left with folders and files, and a terminal at the bottom displaying a command prompt. The right side shows a Chat and CLAude Code sections of the VS Code interface.


## Frame 36 — t=52:30

The screenshot is from VS Code, a popular code editor. The focus is on a project titled "MoneyClaw," with a README.md file open. The code includes instructions for setting up a Google API credentials, specifically for the Gmail API. The terminal at the bottom shows a a user is in a Miniconda3 environment. The chat window on the right shows a a user is interacting with others, discussing the project and its components.


## Frame 37 — t=54:00

The screenshot shows a a split-screen view. On the left, a a a VS Code editor with a file "README.md" open, displaying code related to setting up a Google API credentials for a Gmail component. On the right, is a Google Cloud Console with a "Welcome" page open, showing the project details and quick access options. The terminal at the bottom left shows a a command line interface with Python and zsh prompts.


## Frame 38 — t=55:30

The screenshot shows a a VS Code window with a Python file open, displaying code related to setting up Google API credentials for a Gmail component. The Chrome browser window is open to the Google Cloud Console, specifically on the "Create OAuth client ID" page. The visible text includes steps for creating a new project, enabling the Gmail API, and downloading a credentials JSON file. The layout includes a navigation menu on the left and a main content area on the right, with a Google Cloud Console displaying options for selecting the application type.


## Frame 39 — t=57:00

The screenshot is from VS Code, focusing on a coding tutorial. The visible text includes "Create Flask app with spending optimiz..." and "The Gmail component at /gmail reads emails from your Google accounts using the Gmail API." The terminal shows a "HTTP/1.1 200" and "GET /gmail HTTP/1.1 200". The interface includes a VS Code Explorer, Chat, and CLAIDE CODE tabs, with a sidebar showing project files like "src", "templates", and "app.py".


## Frame 40 — t=58:30

The screenshot shows a a coding tutorial with VS Code in focus. The terminal window displays a Python script with some HTTP request output. The Google Cloud Console is open, showing the "Data Access" section with various API scopes listed. The interface includes a "Filter" field, "Add or remove scopes" button, and a "Manually add scopes" section. The top of the screen shows a a browser tab labeled "Claude Code Prompts."


## Frame 41 — t=60:00

The screenshot is from a VS Code window. The text mentions creating a Flask app with spending optimization and references a src/components/access_gmail.py, fetch_emails(), and Gmail API with OAuth2. The terminal shows a laradj/JPIMPEA932029, zsh, and Python. The chat window includes a message about accessing a Gmail account and using Google Cloud Console setup.


## Frame 42 — t=61:30

The screenshot is from VS Code, focusing on a coding tutorial. The text reads: "MoneyClaw — Access Gmail" and "Fetch Gmail messages whose subject contains a keyword." The interface shows fields for "SUBJECT KEYWORD," "FROM (EMAIL)," "MAX RESULTS," "DATE FROM," and "DATE TO," with a "Run" button. The terminal at the bottom shows Python code and a output of a GET request.


## Frame 43 — t=63:00

The screenshot is from a VS Code window. The focus is on a MoneyClaw project, specifically an "Access Gmail" component. The visible text includes "use the GOOGLE_CREDENTIALS_PATH from .env for accessing my gmail," and "Edit access_gmail.py." The terminal shows a "uv pip install python-dotenv 2>61 | tail -2" and "Installed 1 package in 8ms." The Notion page on the right is titled "AgentWatson" with a goal to optimize money.


## Frame 44 — t=64:30

The screenshot is from a Chrome browser window. The visible text includes "Access Notion," "Search or create Notion pages," "Inputs," "Action," "create," "Search query (for search)," "New page title (for create)," "build with claude code," and "article content [Markdown: # H1, ## H2, - BULLETS, **BOLD**]." The layout shows a a form with fields for inputting search queries, page titles, and article content, alongside a button labeled "Run." The interface is part of a component called "Access Notion" within a a larger application or platform named "ClawCraft."


## Frame 45 — t=66:00

The screenshot shows a a coding environment with VS Code in focus. The visible text includes "MoneyClaw" and "Access Gmail" with fields for "SUBJECT KEYWORD," "FROM (EMAIL)," "MAX RESULTS," and date ranges. The terminal at the bottom shows Python-related commands. On the right, there's a Notion workspace with a title "Issam's Workspace" and sections like "Home," "Recents," and "Agents Beta." The AgentWatson section lists goals such as "My goals are to optimize my money for the year" and "I want to buy a house in year."


## Frame 46 — t=67:30

The screenshot shows a a coding environment with VS Code in focus. The text "AgentWatson" is prominently displayed, along with the goal "My goals are to optimize my money for the year." Below, it states "i want to buy a house in 5 years" and "i want to travel 3 times a year." The interface includes a sidebar with file and directory listings, and a bottom of the screen shows a a terminal with some code and commands.


## Frame 47 — t=69:00

The screenshot shows a a coding environment with VS Code in focus. The text "AgentWatson" is prominently displayed, along with the goal "My goals are to optimize my money for the year." Below, it states, "I want to buy a house in 5 years" and "I want to travel 3 times a year." The interface includes a sidebar with file and directory listings, and a bottom of the screen shows a a a terminal with some code and commands.


## Frame 48 — t=70:30

The screenshot shows a a coding environment with VS Code in focus. The terminal window displays a Python script related to "MoneyClaw" and Gmail access. The Notion app is open, showing a workspace titled "AgentWatson" with goals listed: "My goals are to optimize my money for the year," "I want to buy a house in 5 years," and "I want to travel 3 times a year." The Notion interface includes sections like "Home," "Recents," and "Private," with a sidebar showing workspace navigation.


## Frame 49 — t=72:00

The screenshot is from a VS Code window. The code is in `app.py` and imports Flask, among other libraries. The text includes "import json", "import os", and "import uuid". The Notion window shows a a user interface with text boxes and a sidebar. The terminal at the bottom shows a a Python environment.


## Frame 50 — t=73:30

The screenshot is from a VS Code window. The focus is on a webpage titled "MoneyClaw Components," which describes reusable building blocks for spending optimization. The visible text includes "Components," "Data Connectors," "Gmail," "Fetch transaction emails and receipts from Gmail by keyword or sender," "Google Calendar," "not implemented yet," and "Notion," "Read financial goals and budget pages from your Notion workspace." The interface shows a a a terminal, chat, and code editor, with a sidebar displaying project files and a chat window with multiple participants.


## Frame 51 — t=75:00

The screenshot is from a coding tutorial, focusing on a Notion workspace. The text reads: "AgentWatson. My goals are to optimize my money for the year. I want to travel to Spain on December for 2 week vacation. I want to buy a a house in 5 years. I want to travel 3 times a year. make sure that all your answers are in 3 bullet points." The Notion interface shows a to "Home," "New AI meeting note," and "View all" options. The workspace is titled "Issam's Workspace" and includes sections for "Recents," "Agents Beta," and "Private."


## Frame 52 — t=76:30

The screenshot is from VS Code, focusing on a Python Flask application. The code snippet includes imports and defines routes for landing, components, agent, and chat functionalities. The terminal shows a the debugger is active. The chat window on the right includes a text "Create Flask app with spending optimization" and mentions Telegram integration.


## Frame 53 — t=78:00

The screenshot shows a a coding tutorial in VS Code. The visible text includes "MoneyClaw" and "Telegram Component". The interface displays a chat window with messages about creating a Telegram bot, including instructions to use 'newbot' and to choose a bot name and username. The chat window also shows a a list of commands for managing web apps and games. The bottom of the screen shows a a terminal with debugging information.


## Frame 54 — t=79:30

The screenshot is from VS Code, focusing on a coding environment. The text "Send Telegram" is visible, along with a message input field containing "hola amigo". Below the message field, there's a label "CHAT ID (LEAVE BLANK TO USE DEFAULT)". A button labeled "Run" is present. The terminal shows a "Debugger is active!" and "Debugger PID: 348-412-286". The error message "Failed 'TELEGRAM_BOT_TOKEN'" is displayed prominently.


## Frame 55 — t=81:00

The screenshot is from VS Code, focusing on a Python project named "MoneyClaw." The active file is "send_telegram.py." The code attempts to send a Telegram message with the text "hola amigo" but fails with a error message "400 Client Error: Bad Request for url: https://api.telegram.org/bot/token." The terminal shows the project directory and some debugging information. The chat window on the right shows a a discussion about the code and the error.


## Frame 56 — t=82:30

The screenshot is from a Chrome browser window. The visible text reads: "Stop Overspending. Start Winning. MoneyClaw analyzes your spending, finds waste, and gives you a clear path to financial freedom." Below this, there's a button labeled "Get Started Free." The layout includes sections for "Spending Insights," "Smart Cuts," "Goal Tracker," and "Overspend Alerts," each with a brief description. The overall design is clean and uses a light yellow background with white and orange text.


## Frame 57 — t=84:00

The screenshot shows a a coding environment with VS Code in focus. The visible text includes "MoneyClaw Comp AI Agent" and "Send Telegram". The terminal window displays Python code and a message about server status. The Telegram window shows a a chat with a bot named "AgentWatson" and a user interface with a a list of tasks and a message input field.


## Frame 58 — t=85:30

The screenshot shows a a coding environment with VS Code in focus. The visible text includes "MoneyClaw" and "Send Telegram" in the main interface. The terminal window displays a series of HTTP POST requests. The Telegram window shows a a chat with messages like "dont spend any money" and "Message Sent." The Chrome window displays a webpage titled "Telegram Web" with a chat interface.


## Frame 59 — t=87:00

The screenshot shows a a split-screen view of a coding environment and a a a webpage. The left side displays VS Code with a Python script and terminal output, while the right side shows a a MoneyClaw website with a "Tools (Services)" section. The visible text includes "CLAUDE CODE," "Create Flask app with spending optimi...," and "Send to Telegram." The webpage layout includes headings like "Tools (Services)," "Web Search," "Send to Telegram," "Create Report on Notion," and "Generate Dashboard."


## Frame 60 — t=88:30

The screenshot shows a a coding environment with VS Code in focus. The visible text includes "Interac e-Transfer: You received $45.00 from Sara" and "Interac e-Transfer: Transfer of $128.00 to James." The interface displays a file explorer on the left, a code editor in middle, and a terminal at the bottom. The right side shows a a webpage with sections labeled "Tools (Services)" and options like "Web Search," "Send to Telegram," "Create Report on Notion," and "Generate Dashboard." The bottom right corner includes a a small video call interface with participants named Tanvi, helen, and Eduard Malevskiy.


## Frame 61 — t=90:00

The screenshot is from a VS Code environment. The visible text includes "MoneyClaw Dashboard" and "TOTAL RECEIVED $0.00". The dashboard shows a "TRANSACTIONS 0" and a dropdown menu for "BREAKDOWN BY User". The terminal window shows Python code and debugging output.


## Frame 62 — t=91:30

The screenshot shows a a coding environment with VS Code in focus. The visible text includes "Interac e-Transfer: You received $45.00 from Sara" and "Interac e-Transfer: Transfer of $128.00 to James." The interface displays a file explorer on the left, a code editor in middle, and a terminal at the bottom. The right side shows a a webpage with a title "MoneyClaw — Components," featuring sections like "AI Agent," "Tools (Services)," and "Create Report on Notion." The webpage layout includes buttons and headings related to AI and tools, with a footer indicating "© 2025 MoneyClaw. All rights reserved."


## Frame 63 — t=93:00

The screenshot shows a a coding environment with VS Code in focus. The text "components" is visible at the top of the webpage. The webpage lists "Data Connectors" with options like "Gmail" and "Notion." Below, it mentions "Agent (Brain)" with "Memory" and "Soul" as subcategories, both marked as "not implemented yet." The layout includes a VS Code editor on the left, a terminal window, and a webpage on the right. The webpage has a title "Components" and describes reusable building blocks for spending optimization.


## Frame 64 — t=94:30

The screenshot shows a a coding environment with multiple windows open. The main focus is on a terminal window displaying code related to a Flask app and a chatbot. The top left window is titled "MoneyClaw - Send Telegram," and the top right window is titled "Telegram Web." The visible text includes "Message Sent" and "Delivered to Telegram successfully." The terminal window contains commands and output related to Flask app development and debugging. The layout includes a a chat interface with participants like Tanvi, Farah, Helen, and Eduard Malevskyi.


## Frame 65 — t=96:00

The screenshot shows a a coding environment with VS Code in focus. The visible text includes "MoneyClaw - Web Search," "Components," and "AI Agent." The interface displays a web search functionality with a "QUERY" field containing "netflix" and a "Run" button. The terminal window shows Python code and debugging information. The right side of the screen lists "Data Connectors" with options like Gmail, Google Calendar, and Notion, and an "Agent (Brain)" section with "Memory" noted as "not implemented yet."


## Frame 66 — t=97:30

The screenshot shows a a coding environment with VS Code in focus. The visible text includes "MoneyClaw" and "Web Search" from a webpage. The terminal window displays Python code and terminal commands. The webpage has a yellow header with "MoneyClaw" and a search bar labeled "QUERY" with the text "netflix." The layout includes a search bar, a "Run" button, and a description of the Brave Search API key setup.


## Frame 67 — t=99:00

The screenshot shows a a coding environment with VS Code in focus. The visible text includes "MoneyClaw" and "Web Search" with a query field for "netflix". The interface displays a terminal window with Python code, a chat window with a user named "Tanvi", and a webpage titled "MoneyClaw Web Search" with a "Run" button. The webpage also shows a a search result for "Netflix" with a link to a YouTube video.


## Frame 68 — t=100:30

The screenshot shows a a coding tutorial in VS Code. The visible text includes instructions for setting up Google API credentials and a Notion component. The terminal window displays Python code and debugging output. The right side of the screen shows a a webpage titled "MoneyClaw Workflows," featuring a chat with AI on Telegram feature and a Notion component for summarizing spending. The webpage layout includes headings, buttons, and sections for different components and workflows.


## Frame 69 — t=102:00

The screenshot shows a a coding environment with VS Code in focus. The code is for a Flask app with spending optimization, titled "chat_with_ai_on_telegram.py". The visible text includes imports and function definitions related to Telegram API calls and AI agent interactions. The terminal shows a a Python script running, with a message about port usage. The right side of the screen displays a webpage titled "MoneyClaw Workflows," featuring a chat with AI on Telegram feature and a summary of spending from Gmail to Telegram.


## Frame 70 — t=103:30

The screenshot is from a VS Code coding environment. The code is for a Flask app with a Telegram bot integration. The visible text includes "import requests", "from src.claude import Build_agent", and "from src.components.send_Telegram import send". The terminal shows a "python" and "python -m". The right side of the screen shows a a Zoom meeting interface with participants' names and video feeds.


## Frame 71 — t=105:00

The screenshot shows a a Chrome browser window. The visible text includes definitions and examples related to integration and differentiation in calculus, such as "Integration is the process of finding the integral of a function, which represents the accumulation of quantities, such as areas under a curve." The interface includes a chatbot labeled "AgentWatson" and a title "MoneyClaw Chat with AI on Telegram." The layout features a chat window on the left, a "MoneyClaw" section in center, and a "Components" section on the right.


## Frame 72 — t=106:30

The screenshot shows a a split-screen view. On the left, VS Code is in primary focus, displaying code related to a project named "summarize_interac_spending.py". The code includes imports and functions for fetching emails and summarizing transactions. On the right, a Notion page is visible, with sections labeled "Google Calendar", "Soul", "AI Agent", and "Create Report on Notion". The Notion page includes checkboxes indicating tasks like "not implemented yet". The top right corner shows a a video call interface with participants.


## Frame 73 — t=108:00

The screenshot shows a a coding environment with VS Code in focus. The visible text includes "import os" and "from langchain_core.messages import Role". The interface displays a "MoneyClaw" application with a "Summarize Interac Spending" section. The UI includes a dropdown for selecting a model, month, year, and max messages, with buttons labeled "Run" and "Listen to Summary". The right side of the screen shows a a a Zoom video call interface with participants' names visible.


## Frame 74 — t=109:30

The screenshot is from a Chrome browser window. The visible text includes "Reusable building blocks you can wire into your spending optimization flow." The layout shows sections labeled "Data Connectors," "Agent (Brain)," and "Tools (Services)." Each section contains icons and brief descriptions of features like "Gmail," "Google Calendar," "Notion," "Memory," "Soul," "AI Agent," "Web Search," "Send to Telegram," "Create Report on Notion," and "Generate Dashboard." The interface appears to be a a coding tutorial or documentation for a financial management tool.


## Frame 75 — t=111:00

The screenshot is from a Chrome browser window. The visible text includes "MoneyClaw Workflows" and "End-to-end automations built from components." There are two workflow options displayed: "Chat with AI on Telegram" and "Summarize Interac Spending from Gmail to Telegram." The layout shows a a simple webpage with a a sidebar on the right displaying user profiles.

